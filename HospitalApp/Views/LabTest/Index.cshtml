@model List<HospitalApp.Core.Application.ViewModels.LabTest.LabTestViewModel>


@{
    ViewData["Title"] = "Pruebas";
}

<div class="container mt-5">
    <h3 class="text-center mb-4">Pruebas</h3>
    <hr />
    <div class="row">
        <div class="col">
            <div class="row mb-3">
                <div class="col text-end">
                    <a asp-controller="LabTest" asp-action="Create" class="btn btn-sm btn-success">
                        Nuevo <i class="fas fa-plus"></i>
                    </a>
                </div>
            </div>
            <div>
                <div class="table-responsive">
                    @if (Model == null || !Model.Any())
                    {
                        <div class="alert alert-danger text-center" role="alert">
                            No se encontraron pruebas
                        </div>
                    }
                    else
                    {
                        <table class="table table-success table-striped mx-auto">
                            <thead>
                                <tr>
                                    <th>Nombre</th>
                                    <th class="text-end">Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.Name</td>
                                        <td class="text-end">
                                            <a class="me-3" asp-controller="LabTest" asp-action="Edit" asp-route-id="@item.Id">
                                                <i class="fas fa-edit text-black"></i>
                                            </a>
                                            <a asp-controller="LabTest" asp-action="Delete" asp-route-id="@item.Id">
                                                <i class="fas fa-trash text-black"></i>
                                            </a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
